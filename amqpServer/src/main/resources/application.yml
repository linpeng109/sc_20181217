server:
  port: 8890
spring:
  application:
    name: AmqpServer
  boot:
    admin:
      client:
        enabled: true
  profiles:
    active: native
logging:
  level:
    root: error
    com.cn: debug
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
management:
  endpoints:
    web:
      exposure:
        exclude: "*"
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: always
