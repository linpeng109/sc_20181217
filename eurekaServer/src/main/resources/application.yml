server:
  port: 8761
spring:
  application:
    name: EurekaServer
  profiles:
    active: native
  devtools:
    add-properties: false
    restart:
      enabled: true
  boot:
    admin:
      client:
        enabled: true
eureka:
  server:
    peer-node-read-timeout-ms: 1000
  client:
    fetch-registry: false
    register-with-eureka: false
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: always
logging:
  level:
    root: error
    com.cn: debug